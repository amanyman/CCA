import { ProviderLayout } from '../../components/provider/ProviderLayout';
import { ReferralList } from '../../components/provider/ReferralList';

export function ProviderReferrals() {
  return (
    <ProviderLayout title="Referrals">
      <ReferralList />
    </ProviderLayout>
  );
}
